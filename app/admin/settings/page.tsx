// Admin settings page - simple placeholder
import { redirect } from 'next/navigation'

export default function AdminSettingsPage() {
  // For now, just show settings or redirect to general settings
  redirect('/settings')
}
